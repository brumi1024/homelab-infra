---
profile: production
offline: true

exclude_paths:
  - .cache/
  - .github/
  - .venv/

mock_modules:
  - community.general.onepassword

mock_roles:
  - geerlingguy.docker
  - geerlingguy.security
  - bpbradley.komodo

extra_vars:
  ansible_user: root
  ansible_host: example.com
