---
- name: Install git for the sanoid source checkout
  ansible.builtin.apt:
    name: git
    state: present
  tags: zfs

- name: Install sanoid's perl dependencies
  ansible.builtin.apt:
    name:
      - libcapture-tiny-perl
      - libconfig-inifiles-perl
    state: present
  tags: zfs

# The Debian package was orphaned 2026-08-31 and Debian 13 ships 2.2.0;
# the pinned upstream tag keeps sanoid current without a third-party repo.
- name: Check out the sanoid source at the pinned tag
  ansible.builtin.git:
    repo: https://github.com/jimsalterjrs/sanoid.git
    dest: "/usr/local/src/sanoid-{{ zfs_host_sanoid_version }}"
    version: "{{ zfs_host_sanoid_version }}"
    depth: 1
  tags: zfs

- name: Check whether the sanoid source checkout exists
  ansible.builtin.stat:
    path: "/usr/local/src/sanoid-{{ zfs_host_sanoid_version }}/sanoid"
  register: zfs_host_sanoid_source
  check_mode: false
  tags: zfs

- name: Report the sanoid install a dry run cannot show without the checkout
  ansible.builtin.debug:
    msg: >-
      sanoid {{ zfs_host_sanoid_version }} would be installed to /usr/local/sbin
      and /etc/sanoid; the copies need the checkout above and are skipped in check mode.
  changed_when: true
  when:
    - ansible_check_mode
    - not zfs_host_sanoid_source.stat.exists
  tags: zfs

- name: Install sanoid, syncoid, and findoid
  ansible.builtin.copy:
    src: "/usr/local/src/sanoid-{{ zfs_host_sanoid_version }}/{{ item }}"
    dest: "/usr/local/sbin/{{ item }}"
    remote_src: true
    owner: root
    group: root
    mode: "0755"
  loop:
    - sanoid
    - syncoid
    - findoid
  when: zfs_host_sanoid_source.stat.exists
  tags: zfs

- name: Ensure /etc/sanoid exists
  ansible.builtin.file:
    path: /etc/sanoid
    state: directory
    owner: root
    group: root
    mode: "0755"
  tags: zfs

- name: Install the upstream sanoid defaults
  ansible.builtin.copy:
    src: "/usr/local/src/sanoid-{{ zfs_host_sanoid_version }}/sanoid.defaults.conf"
    dest: /etc/sanoid/sanoid.defaults.conf
    remote_src: true
    owner: root
    group: root
    mode: "0644"
  when: zfs_host_sanoid_source.stat.exists
  tags: zfs

- name: Template sanoid.conf from the configured datasets
  ansible.builtin.template:
    src: sanoid.conf.j2
    dest: /etc/sanoid/sanoid.conf
    owner: root
    group: root
    mode: "0644"
  tags: zfs

# Adjusted from packages/debian/sanoid*.service and sanoid.timer upstream:
# ExecStart points at /usr/local/sbin since the Debian package is not used.
- name: Install sanoid systemd units
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ item }}"
    owner: root
    group: root
    mode: "0644"
  loop:
    - sanoid.service
    - sanoid.timer
    - sanoid-prune.service
  notify: Reload systemd for zfs_host
  tags: zfs

- name: Apply systemd unit changes before enabling the timer
  ansible.builtin.meta: flush_handlers
  tags: zfs

- name: Check whether sanoid.timer is installed
  ansible.builtin.stat:
    path: /etc/systemd/system/sanoid.timer
  register: zfs_host_sanoid_timer_unit
  check_mode: false
  tags: zfs

- name: Report the pending sanoid.timer enable a dry run cannot perform
  ansible.builtin.debug:
    msg: sanoid.timer would be enabled and started once its unit file is installed.
  changed_when: true
  when:
    - ansible_check_mode
    - not zfs_host_sanoid_timer_unit.stat.exists
  tags: zfs

- name: Enable and start sanoid.timer
  ansible.builtin.systemd:
    name: sanoid.timer
    enabled: true
    state: started
  when: zfs_host_sanoid_timer_unit.stat.exists
  tags: zfs
