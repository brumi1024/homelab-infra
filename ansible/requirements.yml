---
# Ansible requirements for Homelab Komodo infrastructure
# Install with: ansible-galaxy install -r requirements.yml

# External roles for infrastructure management
roles:
  # Docker installation and management
  - name: geerlingguy.docker
    version: "8.0.0"

  # Pragmatic SSH, fail2ban, and unattended-upgrades baseline
  - name: geerlingguy.security
    version: "3.0.2"

  # Komodo periphery deployment with systemd
  - name: bpbradley.komodo
    version: "v2.1"

# Required collections
collections:
  # Docker management modules
  - name: community.docker
    version: ">=3.4.0"

  # General utilities including 1Password lookups
  - name: community.general
    version: ">=8.0.0"

  # Proxmox API modules for LXC creation and snapshots; LXC rootfs resize
  # remains a pct-over-SSH fallback in the proxmox_guest role.
  - name: community.proxmox
    version: ">=2.0.0"
